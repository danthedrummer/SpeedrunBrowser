<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.ddowney.speedrunbrowser.MainActivity">

    <item
        android:id="@+id/action_search"
        android:orderInCategory="100"
        android:title="@string/action_search"
        android:icon="@drawable/ic_search"
        app:showAsAction="collapseActionView|ifRoom"
        app:actionViewClass="android.support.v7.widget.SearchView" />

    <item
        android:id="@+id/action_clear_all"
        android:orderInCategory="100"
        android:title="@string/clear_all_filters"
        app:showAsAction="ifRoom"
        android:icon="@drawable/ic_clear_all"
        android:visible="true"/>

    <item
        android:id="@+id/action_show_favourites"
        android:orderInCategory="100"
        android:title="@string/favourites"
        app:showAsAction="ifRoom"
        android:icon="@drawable/ic_favorite_border"/>

</menu>
